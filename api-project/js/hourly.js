const hours = {
    zero:
}

function one(min, max) {
  min = Math.ceil(4);
  max = Math.floor(6);
  return Math.floor(Math.random() * (max - min + 1) + min);
}

function one(min, max) {
  min = Math.ceil(4);
  max = Math.floor(6);
  return Math.floor(Math.random() * (max - min + 1) + min);
}

function one(min, max) {
  min = Math.ceil(4);
  max = Math.floor(6);
  return Math.floor(Math.random() * (max - min + 1) + min);
}
